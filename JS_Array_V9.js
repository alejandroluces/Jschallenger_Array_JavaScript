// Ordenar una matriz de cadenas alfabéticamente

// Write a function that takes an array of strings as argument. 
// Sort the array elements alphabetically. Return the result.

// Escriba una función que tome una matriz de cadenas como argumento. 
// Ordene los elementos de la matriz alfabéticamente. Devuelve el resultado.


const myFunction =(arr) =>{

    return arr.sort()

}

console.log(myFunction(['b', 'c', 'd', 'a']));
console.log(myFunction(['z', 'c', 'd', 'a', 'y', 'a', 'w']));

